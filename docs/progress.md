# Прогресс проекта

## Текущий статус

Выполнены этапы **00 — документационная инициализация**, **01 — согласование стека и архитектуры**, **02 — каркас приложения и модель предметной области** и **03 — расчёт доступности**. Также выполнены **04.1 — серверное создание записи и отмена по защищённой ссылке** и **04.2 — клиентский интерфейс онлайн-записи**. Telegram и отправка уведомлений остаются этапом 06; весь MVP ещё не завершён.

Результат 04.2 принят пользователем после повторной проверки. **05.1 прошла отдельную проверку и принята пользователем.** В отдельной проверке 05.2 обнаружен дефект согласования условий услуги; исправление реализовано и прошло полный повторный набор проверок (264 Vitest, 53 E2E). **Этап 05.2 и исправление согласования условий прошли отдельную проверку и приняты пользователем.** Рабочие учётные данные не создавались. Ограничения проверки и открытый вопрос npm audit сохранены; пользователь разрешил фиксацию 05.2 и отправку в origin/main после проверки актуальной удалённой ветки.

**05.3 — управление расписанием прошло независимую проверку и принято пользователем (300 Vitest, 65 E2E).** Подробности и полный список изменённых файлов — в разделе 05.3 ниже. Пользователь разрешил фиксацию и push в origin/main после проверки актуальности удалённой ветки; force push запрещён.

**05.4 — настройки времени и горизонта прошли независимую проверку и приняты пользователем: 377 unit/integration и 85 E2E успешно.** ADR-0009 переведён в Accepted. Пользователь разрешил фиксацию только файлов 05.4 и push в origin/main после проверки актуальности ветки; при расхождении необходимо остановиться, force push запрещён. Ограничения и открытый вопрос npm audit сохраняются. Подробности и точка продолжения — в конце документа.

**05.5 — журнал записей и управление статусами выполнен и принят пользователем после исправления замечания независимой проверки: 454 unit/integration и 103 E2E успешно.** Исходный коммит b701bb732682d8e638e19e506610e6daa350c2f2. Актуальная точка продолжения 05.5 — в конце документа; следующий этап не начат.

**05.6 — ручное создание записи администратором принято после независимой проверки.** Исходный HEAD и origin/main: `f32f86d0e10156780c6cbd5aa0c0f9923718d4f6`; исходное дерево было чистым. Схема/миграции и зависимости не менялись, ADR-0011 переведён в Accepted. Результаты полного последовательного прогона и разрешённая фиксация приведены в разделе 05.6 ниже; 05.7.1 и 05.7.2 уже приняты.

**05.7.1 — административное исправление имени и телефона прошло независимую проверку и принято пользователем: 538/538 unit/integration и 124/124 E2E успешно; build, lint, typecheck, format:check и docker compose config успешны.** Исходный HEAD и origin/main: 75b7d0db07d6db68e9ffd8a34e73bf3138518372, исходное дерево было чистым. Prisma-схема, миграции, зависимости, рабочая БД и Docker volumes не менялись; ADR-0012 переведён в Accepted.

**05.7.2 — административное изменение параметров визита и перенос прошла независимую проверку и принята пользователем: 602/602 unit/integration и 133/133 E2E успешно.** Подтверждены KEEP_CURRENT/CATALOG, SPECIFIC/ANY, транзакционный перенос, конкурентность, safe unknown outcome и полная навигация. Исходный HEAD и origin/main: c7c6d91e1a10eeee895de9498f9b946ea3b3d01a. ADR-0013 переведён в Accepted; пользователь разрешил фиксацию ровно 23 перечисленных файлов одним коммитом и обычный push в origin/main. Telegram/outbox, медиа и deployment не начаты.

**06.1 выполнен только как архитектурное проектирование:** подготовлены Proposed ADR-0014 и отдельный технический план Telegram-подключений, transactional outbox и блоков 06.2–06.5. Telegram ещё не реализован; код, Prisma schema, миграции, зависимости, Docker и worker не менялись.

### Выполнено

- Зафиксирована подтверждённая концепция продукта «Запросто».
- Описаны границы MVP, бизнес-правила и основные пользовательские сценарии.
- Утверждены обязательные контактные данные, правила отмены и переноса, горизонт бронирования и работа с часовыми поясами.
- Утверждено назначение доступных услуг мастерам; индивидуальные цена и длительность услуги исключены из MVP.
- Утверждён алгоритм «Любой мастер» по забронированным минутам, количеству записей и административному порядку.
- Утверждены правила проверки и нормализации российского телефона.
- Утверждены ручное создание и редактирование записей администратором и результаты визита.
- Утверждены Telegram-уведомления и исключение SMS из MVP.
- Утверждены правила уведомления клиента при административном изменении записи.
- Утверждена ручная передача текста подтверждения и защищённой ссылки для телефонной или офлайн-записи.
- Утверждены мягкая отмена, освобождение слота и хранение данных отмены.
- Утверждены состояния записи, допустимые переходы и история изменения статуса.
- Зафиксирована модель отдельной однофилиальной установки для каждого покупателя.
- Утверждены Node.js 24 LTS, Next.js, React, TypeScript strict, Tailwind CSS, Zod, PostgreSQL 17, Prisma ORM и Prisma Migrate.
- Утверждены Vitest, Playwright и Docker Compose для локальной разработки.
- Выбран модульный Next.js-монолит без отдельного backend; worker определён как отдельный процесс того же приложения и образа.
- Зафиксирован совокупный источник истины схемы из `schema.prisma` и SQL-миграций; `prisma db push` запрещён для production.
- Утверждена защита от пересечений через PostgreSQL exclusion constraint, ручную SQL-миграцию и конкурентный интеграционный тест.
- Отказались от таблицы поминутной занятости `appointment_occupancy`.
- Утверждены недельные рабочие интервалы, датированные исключения и шаг отображаемых времён 15 минут при точном хранении записей.
- Утверждена абстракция файлового хранилища для логотипа и одной фотографии каждого мастера.
- Зафиксированы отдельное развёртывание и отдельная база для каждого барбершопа без зависимости от конкретного облака.
- Демонстрационная среда ограничена вымышленными персональными данными.
- Созданы правила работы над проектом, дорожная карта и журнал архитектурных решений.
- Инициализирован Git-репозиторий и базовые служебные файлы.
- Создан минимальный Next.js 16 App Router-каркас с React, TypeScript strict, Tailwind CSS, ESLint, Prettier и EditorConfig.
- Добавлены стартовая русскоязычная страница и health endpoint `/api/health`.
- Worker добавлен как отдельная точка запуска того же репозитория и Docker-образа без Telegram-логики.
- Добавлены multi-stage Dockerfile и Docker Compose для web, worker и PostgreSQL 17 с health checks и постоянными volumes.
- Создана Prisma-схема подтверждённой предметной модели без `Customer` и `appointment_occupancy`; имя и телефон находятся в `Appointment`, данные услуги сохраняются снимком.
- Моменты записей отображены в PostgreSQL как `timestamptz`, локальные интервалы расписания — как `time`, даты исключений — как `date`.
- Добавлены базовая миграция и ручная SQL-миграция с `btree_gist`, полуоткрытым `tstzrange` и exclusion constraint для всех статусов, кроме `CANCELLED`.
- Реализованы предметные Zod-валидаторы и unit-тесты телефона, цены, длительности и порядка границ интервала.
- Реализован конкурентный интеграционный тест: из двух пересекающихся записей создаётся одна, а отмена освобождает интервал.
- Добавлен Playwright smoke-тест стартовой страницы и health endpoint.
- На Node.js 24.19.0 и npm 11.17.0 успешно выполнены Prisma validate/generate, миграции с нуля, lint, typecheck, unit/integration/E2E-тесты и production build.
- Multi-stage образ собран; контейнеры web, worker и PostgreSQL 17 успешно запущены через Docker Compose, web и БД перешли в healthy, worker подтвердил запуск в логах.
- Реализована независимая от Next.js, React и Prisma предметная логика нормализации, объединения и вычитания полуоткрытых интервалов, а также генерации начал с шагом 15 минут без округления длительности услуги.
- Реализован серверный сервис доступности для конкретного мастера и «Любого мастера»: он учитывает назначения услуг, активность, недельные интервалы, перерывы, `DAY_OFF`, `CUSTOM_HOURS`, неотменённые записи, прошедшее время текущего дня и горизонт бронирования.
- Загрузка расписания и записей выполняется пакетно без N+1; поддержаны повторная проверка выбранного интервала и необязательный `excludeAppointmentId` без создания или изменения `Appointment`.
- Реализован детерминированный выбор «Любого мастера» по забронированным минутам локального дня, количеству записей, `displayOrder` и техническому tie-breaker по `id`.
- Для строгого преобразования IANA-часового пояса и локальной даты в UTC добавлен `temporal-polyfill`. Начала слотов перечисляются в локальном календарном времени: несуществующие и неоднозначные DST-начала пропускаются без автоматического сдвига или дублирования; некорректные границы рабочих интервалов завершают расчёт типизированной ошибкой.
- Добавлены unit-тесты чистой логики и интеграционные тесты с PostgreSQL для расписаний, исключений, перерывов, статусов записей, часовых поясов, DST fallback/spring forward, объединённой доступности, повторной проверки и выбора «Любого мастера».
- Исправлен приоритет исключений перед преобразованием расписания в UTC: `DAY_OFF` не преобразует недельные часы и перерывы, сохраняя дневную загрузку; `CUSTOM_HOURS` заменяет недельные часы, но учитывает недельные перерывы. Добавлены 10 PostgreSQL-регрессий для обоих DST-переходов, строгой проверки действующих границ и доступности остальных мастеров.
- Prisma-схема и миграции на этапе 03 не изменялись; существующий exclusion constraint остаётся окончательной защитой от гонок.
- Реализован серверный сервис клиентского бронирования: проверка входных данных, конкретный мастер и «Любой мастер», повторная проверка через scheduling, актуальные снимки услуги и атомарное сохранение записи, BookingRequest и начальной истории.
- Добавлена идемпотентность с сохранением исходного hash запроса и проверкой секрета попытки; последовательные и параллельные повторы возвращают ту же запись и ссылку, не создают историю и не восстанавливают отменённую запись. Решение описано в ADR-0003.
- Добавлены просмотр подтверждения по hash токена и подтверждённая клиентом отмена: только SCHEDULED → CANCELLED, инициатор CLIENT, время и необязательная причина, безопасные повторные и конкурентные вызовы.
- Реальная ошибка exclusion constraint проверена на Prisma/adapter-pg 7.10: P2039 с вложенным SQLSTATE 23P01 и именем appointments_no_overlap; raw SQL при конкурентном повторе даёт P2010 с вложенным TransactionWriteConflict/40001. Остальные ошибки не маскируются под занятое окно.
- Добавлена версионированная миграция 20260830120000_add_booking_request_hash: nullable BookingRequest.requestHash с SQL-проверкой формата; существующие данные, hashes ссылок и старые миграции не переписываются.
- Добавлены unit- и PostgreSQL integration-тесты прикладных сервисов и команда test:postgres с созданием отдельной временной БД для безопасного полного прогона.

### Проверки 04.1 (2026-08-30)

- Полный Vitest-прогон на отдельной PostgreSQL 17 БД: **135 тестов прошли** (82 unit + 53 integration), включая 29 тестов прикладных сервисов бронирования и отмены.
- Проверены реальная форма ошибки Prisma при exclusion conflict, конкурентное бронирование, параллельная идемпотентность, неизменность ссылки, повтор после отмены и откат отмены при сбое сохранения истории.
- Успешны `format:check`, `lint`, `typecheck`, Prisma validate/generate и production build web + worker.
- Playwright: **1 E2E smoke-тест прошёл** на текущей production-сборке через отдельный порт 3104; старый Docker web на порту 3000 не использовался.
- Новая миграция проверена с нуля на временных БД и применена к существующей локальной БД без сброса. Агрегированные отпечатки и количество существовавших строк всех предметных таблиц совпали до и после; исходные данные не изменились.
- Временные тестовые БД удалены. Диагностические файлы задачи убраны; добавлен постоянный безопасный runner `scripts/test-postgres.mjs`.
- Интеграционные проверки выводят неблокирующее предупреждение установленного pg 8 о параллельном `client.query()`. Совместимость будущего pg 9 в эту задачу не входила; версии зависимостей не менялись.
- На момент завершения 04.1 UI, публичные точки входа, защита по частоте и отправка уведомлений ещё не были реализованы. Результаты 04.2 приведены ниже.

### Реализовано в 04.2

- Публичная страница `/`: активные услуги, подходящие активные мастера и «Любой мастер», даты в горизонте, серверные окна scheduling, контакты, итоговая проверка и подтверждение.
- Цена в рублях, длительность, назначенный мастер и даты в часовом поясе бизнеса; смена выбора сбрасывает слот, устаревшие ответы не заменяют актуальные окна.
- Исходный payload с парой из ADR-0003 сохраняется и перечитывается до первого create. Двойной клик блокируется; сетевой сбой и reload продолжают ту же попытку. Неопределённый результат не считается подтверждённым отказом.
- Контакты незавершённого запроса очищаются после 30 минут при следующем исполнении приложения; после успеха остаётся только секрет ссылки. Просроченный/повреждённый маркер не допускает незаметного создания второй записи.
- Страница `/appointment#<token>` читает подтверждение через ClientAppointmentService и показывает актуальный статус. Отмена требует отдельного согласия; причина необязательна, повторная отмена и недопустимый статус обрабатываются.
- Публичные Server Actions проверяют Origin по PUBLIC_ORIGIN; доступность получает отдельный GET endpoint. Секрет находится во fragment и передаётся только в POST-теле: GET, preview и prefetch не отменяют запись.
- Добавлены PostgreSQL rate limits для подготовки, создания, чтения ссылки, отмены и доступности, включая глобальные пределы. Доверие proxy по умолчанию выключено, конфигурация задокументирована.
- Добавлены nonce-CSP, private/no-store, no-referrer, запрет индексации и iframe, безопасные ответы без stack trace/Prisma и отсутствие сторонней аналитики.
- Добавлена миграция `20260830140000_add_public_rate_limits`. Старые миграции не менялись, новых зависимостей нет, существующая БД не сбрасывалась.
- Добавлена ручная команда `demo:seed`: вымышленные услуги, мастера и расписание, без клиентских записей и перезаписи существующих данных.
- Решения и инструкции находятся в [ADR-0004](decisions/0004-public-booking-boundary-and-recovery.md) и [public-booking.md](public-booking.md). Telegram явно остаётся этапом 06.

### Проверки 04.2 (2026-08-30)

- Итоговый полный Vitest-прогон на отдельной PostgreSQL БД: **154 теста прошли** (88 unit + 66 integration). Проверены реальные сервисы, ограничения доступа, атомарность лимита между двумя клиентами БД, отказ при сбое limiter и повторяемость demo:seed.
- Итоговый Playwright на production-сборке: **16 сценариев прошли**. Реальные записи и отмены выполнялись через Server Actions и PostgreSQL; моки использовались только для сетевых сбоев и задержки ответов.
- Проверены конкретный/любой мастер, ошибочный телефон, пустой каталог/мастера/окна, конфликт с сохранением контактов, двойной клик, потеря реального успешного ответа и повтор после reload, неверная ссылка, отмена и недопустимый статус.
- Проверены другой часовой пояс браузера, запоздалые ответы, недоступность хранилища, HTTP 429/Retry-After и отклонение чужого Origin даже с подменённым forwarded host.
- Успешны `format:check`, `lint`, `typecheck`, Prisma validate/generate, production build web + worker, `git diff --check` и `docker compose config --quiet`.
- Chromium/Playwright проверил ширины **360, 390 и 1440 px**, отсутствие горизонтальной прокрутки и видимый фокус. Вручную просмотрены снимки каталога, времени, итоговой проверки, подтверждения и отмены; по результатам исправлены переносы заголовка на 360 px и скрытие skip-link. В полном визуальном сценарии не было ошибок JavaScript.
- Встроенный Browser подключить не удалось из-за ошибки локальной среды; отдельная ручная интерактивная проверка в нём не выполнена. Проверка проведена в настоящем Chromium через Playwright и визуальным просмотром его снимков. Safari/Firefox, физические устройства и production reverse proxy не проверялись.
- Все временные тестовые БД удалены runner-ом; порт тестового сервера 3108 освобождён. Прежний Docker web на 3000 не использовался и не останавливался.
- Новая миграция проверена на отдельных БД и **не применена к существующей локальной БД**. Для демонстрации сначала выполнить prisma:migrate:deploy, затем demo:seed; рабочие данные не затрагивались.
- Остаются неблокирующие предупреждения pg 8 о параллельном client.query и Next.js о next start при output: standalone. E2E завершился успешно; контейнерный production entrypoint по-прежнему использует server.js, сам Docker-образ в 04.2 заново не собирался.
- Commit/push не выполнялись. Следующий этап не начат.

### Исправление skip-link после повторной проверки 04.2 (2026-08-30)

- Переход «К содержимому» заменён на клавиатурно доступную кнопку без якорной навигации. Она вызывает focus() и scrollIntoView() для основного содержимого; секрет fragment и URL не меняются.
- Для main добавлены id=main и tabIndex=-1 на главной, странице защищённой записи и странице ошибки, включая состояние недоступного каталога. Обработка защищённых ссылок и бизнес-правила не менялись.
- E2E-регрессия создаёт реальную тестовую запись через UI и PostgreSQL, открывает её защищённую ссылку в новых вкладках на 360 и 1440 px, активирует переход через Tab → Enter, проверяет неизменный URL, фокус, данные записи и клавиатурное открытие формы отмены. Оба состояния главной проверены на тех же ширинах, включая следующий Tab внутри main.
- Первоначальный новый E2E-тест не прошёл из-за подготовки повторной навигации на тот же fragment: браузер сохранял прежний фокус. Подготовка исправлена новой вкладкой для каждого размера; три целевые регрессии после этого прошли. Неуспешный прогон не засчитан.
- После исправления успешно повторены полный Vitest (154 теста: 88 unit + 66 integration), полный Playwright на production-сборке (19 сценариев), format:check, lint, typecheck и production build web + worker. Визуально просмотрены шесть новых снимков защищённой записи и обоих состояний главной на 360/1440 px; дефектов переноса и перекрытия содержимого не обнаружено.
- Временных тестовых БД и процессов runner/Playwright не осталось, порт 3108 свободен. Существующая БД и посторонние серверы не изменялись. Ограничения браузерной проверки прежние: Chromium, без отдельной проверки Safari/Firefox и физических устройств. Commit/push не выполнялись.

### Реализовано в 05.1 (2026-08-31)

- Серверная аутентификация через существующие AdminUser/AdminSession: Argon2id, случайные непрозрачные токены, только SHA-256 токенов в БД, 12-часовой срок, отзыв и проверка активности при каждом чтении закрытых данных.
- Отдельные PostgreSQL-ключи лимитов входа (10/мин на клиента, 60/мин глобально), 5 ошибок → 15 минут блокировки аккаунта, блокировка строки для конкурентных обновлений и повторная проверка hash после операторского сброса.
- Строгий Origin/Sec-Fetch-Site для входа/выхода, наследование доверия IP только от настроенного proxy, одинаковые ошибки для неверного/неизвестного/неактивного/заблокированного пользователя, dummy Argon2, ограничения ввода до хеширования.
- HTTPS HttpOnly/Secure/SameSite=Lax cookie с __Host-префиксом; отдельная cookie для локального loopback HTTP. Private/no-store, nonce-CSP и безопасный фиксированный возврат на /admin.
- Русскоязычная форма /admin/login и защищённая /admin с настоящим выходом, ожиданием и сообщениями ошибок. Закрытые данные не выдаются анонимному посетителю; авторизация находится у серверных данных, не только в навигации/layout.
- Локальные TTY-команды admin:create и admin:reset: скрытый повторный ввод пароля, запрет аргументов и pipe, безопасный повтор/гонка первого создания, атомарный сброс с отзывом всех сессий, без активации отключённого аккаунта.
- README, архитектура, roadmap, [ADR-0005](decisions/0005-admin-authentication-and-sessions.md) и [инструкция эксплуатации](admin-auth.md) обновлены. Бизнес-правила, Prisma-схема, миграции и Dockerfile не менялись. Следующий этап не начат.

### Проверки 05.1

- Итоговый полный Vitest: **192 теста прошли (111 unit + 81 integration)**. PostgreSQL 17, отдельная случайная тестовая БД, все четыре существующие миграции применены с нуля. Проверены Argon2, вход/отказ, неизвестный/неактивный пользователь, сроки и параллельная блокировка, общий limiter для разных клиентов БД и нового экземпляра сервиса, истёкшие/отозванные/поддельные сессии, деактивация, выход/replay, Origin, DTO и операторские операции.
- Итоговый полный Playwright на production-сборке: **31 сценарий прошёл** (12 административных + 19 существующих публичных). Проверены реальные Actions/PostgreSQL, HTML/RSC без авторизации, cookie, запрет внешнего returnTo, отсутствие секретов в ответах/storage, выход и старый токен, истечение/отзыв/деактивация, CLI без TTY/с аргументами.
- Проверены клавиатурный порядок и «К содержимому», состояния ожидания/ошибок и переполнение на 360/390/1440 px. Визуально просмотрены вход и каркас на 360/1440; после этого увеличен отступ под кнопкой входа, финальный мобильный снимок повторно проверен. Полный прогон сохранил регрессию fragment-ссылки и публичной записи.
- Успешны финальные format:check, lint, production build web + worker, последовательный после build typecheck и git diff --check.
- Multi-stage Docker-сборка итогового кода успешна. Argon2id hash/verify отдельно выполнен в итоговом non-root runner (UID=1001, Node.js 24.20.0) без сети и БД. Локально Node.js 24.19.0. Dockerfile не потребовал дополнительных пакетов.
- Начальные неуспешные прогоны не засчитаны: исправлены тип констант Argon2 и два тестовых типа; unit ожидал другой порядок PHC-параметров; два E2E ошибочно считали пустой live-region Next.js пользовательским alert. После уточнения ожиданий прошли все 12 административных, затем все 31 E2E.
- После финального прогона временных zaprosto_test_* БД нет, порт 3108 освобождён.
- В тестах использовались только изолированные БД и вымышленные аккаунты. Рабочие учётные данные не создавались; рабочая БД и volumes не очищались. Traces выключены, снимки формы сделаны без пароля. Commit/push не выполнялись.

### Ограничения проверки 05.1

- Нет проверки Firefox/Safari, физических устройств, production-домена с реальным TLS/reverse proxy и внешнего pentest. HTTPS-cookie policy проверена unit-тестом, браузерный E2E — локальный HTTP.
- Скрытый TTY-ввод проверен unit-тестами (echo, backspace, отмена, переполнение, EOF); non-TTY/аргументы — настоящим subprocess. Создание/повтор/гонки/сброс — интеграционно на реальном PostgreSQL. Ручной ввод в физическом терминале и создание реального аккаунта не выполнялись.
- npm audit и npm audit --omit=dev сообщают **3 high** в существующей цепочке Prisma → @prisma/config → deepmerge-ts ([GHSA-ggr8-5vv4-36mx](https://github.com/advisories/GHSA-ggr8-5vv4-36mx)). Argon2 не указан в отчёте; автоматический major downgrade/force fix не применялся. Нужен отдельный разбор зависимости.
- Сохраняются прежние предупреждения pg 8 о параллельных client.query и next start при output=standalone; тесты проходят. Сам Docker runner запускается через server.js, как предусмотрено Dockerfile.
- Без MFA остаются риски фишинга/скомпрометированного устройства. При атаке общий limiter/блокировка могут временно ограничить легитимный вход; нужны ограничения трафика на production proxy. Автоматическая очистка старых AdminSession и управление бизнес-данными в этап не входят.

### Не выполнено

- Управление расписанием и записями через административный UI; каталог и мастера реализованы в 05.2.
- Telegram Bot API, обработка outbox и отправка уведомлений.
- Загрузка изображений через UI и production-развёртывание.

## Реализовано и проверено в 05.2 (2026-08-31)

**Этап 05.2, включая исправление публичного согласования условий, принят пользователем после отдельной проверки.** Разрешены коммит и отправка в origin/main; ограничения проверки и открытый вопрос npm audit сохраняются.

- /admin/services: список, создание/редактирование, цена в рублях с точным переводом через BigInt в копейки, целая положительная длительность без кратности 15, активность и «Выше/Ниже». Уникальность названий не вводилась.
- /admin/masters: имя, описание, активность, атомарный набор назначений, порядок. Неактивные назначения явно показаны и сохраняются. Мастер без услуг допустим. Новый мастер не получает график; photoMediaId/фото/URL не принимаются и существующие связи не затираются.
- Техническая миграция добавляет Service.version и Master.version. Advisory lock и версии защищают от двух вкладок, ABA и конкурирующего порядка; ID и весь запрос проверяются до записи. Потеря результата не подтверждается как успех и не вызывает автоматического повтора.
- Каждая серверная операция использует действующую сессию 05.1; повторная проверка после ожидания замка учитывает отзыв/истечение/деактивацию. Сохранены Origin/Sec-Fetch-Site, private/no-store, nonce-CSP, полная безопасная навигация, текстовый вывод и skip-link.
- Каталог читается публичной страницей без кеша; устаревший клиент повторно проверяется существующим booking. Записи, снимки услуги, интервалы и история не меняются; прежние ссылки и отмена работают. Алгоритм «Любой мастер», exclusion constraint и публичное восстановление не переписывались.
- Обновлены README, архитектура, roadmap и инструкция входа; добавлены [эксплуатация каталога](admin-catalog.md) и [ADR-0006](decisions/0006-admin-catalog-concurrency.md).

### Первоначальные проверки 05.2 (до обнаружения дефекта)

- Полный Vitest на настоящем PostgreSQL 17: **238/238 (139 unit + 99 integration)**, 22 файла. Включены 28 новых unit и 18 новых integration. Проверены атомарные назначения (включая реальный SQL-trigger отказ после начала сохранения), неверные ID, деактивация/активация, фото и отсутствие графика, версии/ABA/гонки порядка, выбор «Любого мастера», неизменность записей и снимков, replay/ссылка/отмена, отказ новым записям и повторная авторизация после ожидания.
- Полный Chromium/Playwright на production-сборке: **43/43** (12 новых + все 31 регрессии 04.2/05.1). Реальные Actions и PostgreSQL; моделировались только сетевые сбои и задержки. Проверены прямые POST каждой новой Action без/с истёкшей/отозванной сессией, отключённым администратором и чужим Origin; подстановка полей, HTML как текст, две вкладки и потеря настоящего успешного ответа.
- E2E проверил 360/390/1440 px, клавиатуру, фокус, «К содержимому», пустые состояния, ошибки и ожидание. Визуально просмотрены 8 снимков списков/форм услуг и мастеров на 360 и 1440 px: переполнения, обрезки и перекрытий не обнаружено. Снимки находятся в игнорируемом test-results; браузерные traces выключены.
- Успешны format:check, lint, production build web + worker, typecheck строго после build/Prisma generate, git diff --check. Миграции, включая новую, применены runner-ом с нуля через Prisma Migrate в изолированных БД.
- Неуспешные первоначальные прогоны не засчитаны: исправлены вызов advisory lock через executeRaw (Prisma не десериализует void), имена методов в новых тестах; подсказки отделены от label для стабильных доступных имён. Прямым тестовым POST явно передаётся сохранённая cookie. После исправлений прошли целевые и полные прогоны.
- После финального E2E read-only проверка: _\*0 временных zaprosto_test_* БД_*, порт **3108 свободен**. Существующая локальная БД, реальные аккаунты, volumes и посторонние серверы не менялись. Миграция к рабочей БД **не применена**.
- Commit/push не выполнялись. Следующий этап не начат.

### Исправление по результатам отдельной проверки 05.2

Обнаружен и воспроизведён дефект: клиент видел 1500 ₽ / 35 минут, а новая запись молча использовала изменённые условия. До исправления три независимых integration-регрессии падали: цена менялась на 2500 ₽, длительность — на 60 минут при свободном интервале, название — на «Новое название», но сервер каждый раз возвращал успех. Браузерная регрессия цены также падала: после подтверждения старого экрана не было отказа и повторного согласования.

- В публичный каталог добавлен SHA-256 отпечаток id/name/priceKopecks/durationMinutes. Создание передаёт expectedServiceTerms именно показанного экрана. Service.version остаётся административной версией.
- Проверка отпечатка выполняется в Serializable-транзакции **после replay** и до scheduling/Appointment. Несовпадение или отсутствие для новой попытки откатывает BookingRequest, Appointment и историю.
- SERVICE_TERMS_CHANGED возвращает актуальные название, цену, длительность и свежую доступность из одного RepeatableRead-снимка. UI показывает их, очищает слот, запрашивает окна заново и требует явной кнопки повторного подтверждения. Автоматической отправки новых условий нет.
- booking-v2 включает исходный отпечаток; booking-v1 сохранён байтово для replay старых строк. sessionStorage сохраняет прежний ключ и буквально повторяет старые payload без добавления свежей версии. Потеря ответа и конфликт hash оставляют блокировку новой записи.
- Прежние снимки, строки requestHash, история и защищённые ссылки не переписываются. Изменение требует только кода; дополнительной миграции данных нет.
- Добавлены **23 независимые integration-регрессии**, **10 E2E** и **3 unit-проверки совместимости**. Цена, длительность, название, повторное согласование, потеря ответа → изменение каталога → replay, старый/неполный формат проверены отдельно от административного набора.
- Полный повторный Vitest: **264/264 (142 unit + 122 integration)**, 23 файла. Полный финальный Chromium/Playwright на production-сборке: **53/53**, включая все 43 прежних сценария и 10 новых.
- Первый полный E2E-прогон после исправления дал 51/53: две старые проверки ожидали прежнее общее сообщение при одновременном изменении условий и отключении мастера/назначения. Ожидания обновлены для SERVICE_TERMS_CHANGED, добавлены проверки новых условий, отсутствия доступного окна и частичных строк. Падения не засчитаны как успешная проверка.
- Визуально просмотрены 3 снимка: обновлённая длительность на 360 px и повторное подтверждение новой цены на 360/1440 px. Актуальные условия и явная кнопка видны, перекрытий и обрезки нет; E2E также проверил отсутствие горизонтального переполнения.
- Финальные production build web + worker, затем отдельный typecheck выполнены последовательно и успешны; lint, format:check и git diff --check успешны. Все unit/integration и E2E выполнялись на случайных отдельных PostgreSQL-БД.
- После завершения read-only проверка подтвердила **0 временных тестовых БД**, порт **3108 свободен**, HEAD неизменен. Рабочая БД и реальные аккаунты не изменялись; commit/push и следующий этап не выполнялись.
- Контракты и решение обновлены в [booking-service.md](booking-service.md), [public-booking.md](public-booking.md) и [ADR-0007](decisions/0007-booking-service-terms.md).

### Ограничения проверки 05.2

- Проверен Chromium на локальном HTTP. Safari/Firefox, физические устройства, screen reader, production TLS/reverse proxy, внешний pentest и повторная Docker image-сборка в 05.2 не проверялись.
- Инструменты обычного shell/просмотра изображений не запускались из-за ошибки настройки Windows sandbox; команды выполнены через одобренный запуск вне песочницы. PNG прочитаны напрямую без изменения через этот же доступ. Отдельная интерактивная проверка встроенным браузером не выполнялась.
- При неизвестном результате административного создания требуется ручная сверка списка. Автоповтора нет; осознанное повторное создание после проверки может породить дубликат, exactly-once для административного создания не заявляется.
- Сохраняются предупреждения pg 8 о параллельных client.query и next start при output=standalone. Сборка web/worker и тесты проходят; Docker entrypoint не менялся.
- Вопрос **npm audit: 3 high в прежней цепочке Prisma → @prisma/config → deepmerge-ts** остаётся открытым по отчёту 05.1. Зависимости не менялись, повторный audit и force fix не выполнялись. Прежние ограничения 05.1 (MFA, proxy, очистка сессий) сохраняются.

## Следующий шаг

Приёмка 05.4 завершена после независимой проверки (377 unit/integration, 85 E2E). Пользователь разрешил коммит `feat: add business time settings and booking context protection` и push в origin/main после проверки отсутствия расхождения веток. Force push запрещён. Ограничения проверок и открытый вопрос npm audit сохраняются. Рабочая БД и её миграции не затрагиваются; следующий этап без отдельного поручения не начинается.

## Изменённые файлы 05.2

43 файла с учётом исправления согласования условий, включая новые, без generated/test artifacts:

- `README.md`
- `docs/admin-auth.md`
- `docs/admin-catalog.md`
- `docs/architecture.md`
- `docs/progress.md`
- `docs/roadmap.md`
- `docs/decisions/0006-admin-catalog-concurrency.md`
- `prisma/schema.prisma`
- `prisma/migrations/20260831150000_catalog_versions/migration.sql`
- `src/app/admin/page.tsx`
- `src/app/admin/catalog-actions.ts`
- `src/app/admin/services/page.tsx`
- `src/app/admin/masters/page.tsx`
- `src/app/globals.css`
- `src/components/admin/catalog-editor.tsx`
- `src/components/admin/catalog-page.tsx`
- `src/components/admin/navigation.tsx`
- `src/modules/auth/server/auth-service.ts`
- `src/modules/catalog/domain/admin-input.ts`
- `src/modules/catalog/domain/admin-input.unit.test.ts`
- `src/modules/catalog/server/admin-catalog.ts`
- `src/server/admin/catalog-boundary.ts`
- `src/server/admin/catalog.ts`
- `tests/integration/admin-catalog.test.ts`
- `tests/e2e/admin-catalog.spec.ts`

- `docs/booking-service.md`
- `docs/public-booking.md`
- `docs/decisions/0003-booking-idempotency-and-protected-links.md`
- `docs/decisions/0007-booking-service-terms.md`
- `src/components/booking/booking-flow.tsx`
- `src/modules/catalog/server/service-terms.ts`
- `src/modules/booking/domain/booking-input.ts`
- `src/modules/booking/domain/booking-input.unit.test.ts`
- `src/modules/booking/server/booking-security.ts`
- `src/modules/booking/server/booking-service.ts`
- `src/modules/booking/server/types.ts`
- `src/modules/booking/client/attempt-storage.unit.test.ts`
- `src/server/public/catalog.ts`
- `tests/integration/booking-terms.test.ts`
- `tests/integration/booking-service.test.ts`
- `tests/integration/public-boundary.test.ts`
- `tests/e2e/booking-terms.spec.ts`
- `tests/e2e/booking.spec.ts`

## История этапов

| Этап                                               | Статус           | Результат                                                                                                                                                              |
| -------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 00 — Инициализация проекта и документация          | Выполнен         | Создана документационная основа; код приложения отсутствует                                                                                                            |
| 01 — Согласование стека и архитектуры              | Выполнен         | Принят ADR-0002 со стеком, моделью развёртывания и защитой от двойного бронирования                                                                                    |
| 02 — Каркас приложения и модель предметной области | Выполнен         | Созданы приложение, worker, Prisma-модель, миграции, Docker Compose и тестовая инфраструктура; защита от пересечений подтверждена конкурентным тестом                  |
| 03 — Расчёт доступности                            | Выполнен         | Реализованы чистая интервальная логика, серверный расчёт окон, строгая работа с локальной датой и UTC, повторная проверка и детерминированный алгоритм «Любой мастер»  |
| 04.1 — Создание и отмена записи на сервере         | Выполнен         | Транзакционное бронирование, идемпотентность, постоянная защищённая ссылка, просмотр подтверждения, безопасная клиентская отмена и тесты на PostgreSQL                 |
| 04.2 — Клиентский интерфейс записи                 | Выполнен         | Последовательный UI, безопасный повтор, защищённая отмена, публичные границы, PostgreSQL rate limits, демокоманда и проверки                                           |
| 05.1 — Аутентификация администратора               | Выполнен, принят | Argon2id, серверные сессии, PostgreSQL-защита, TTY CLI и защищённый каркас; 192 Vitest и 31 E2E                                                                        |
| 05.2 — Услуги, мастера и назначения                | Выполнен, принят | Защищённый каталог, атомарные назначения/порядок, версии; 264 Vitest и 53 E2E                                                                                          |
| 05.3 — Расписание, перерывы и исключения           | Выполнен, принят | Защищённый редактор, атомарные операции и общий Master.version; 300 Vitest и 65 E2E. Независимая проверка пройдена.                                                    |
| 05.4 — Настройки времени и горизонта               | Выполнен, принят | Версия настроек, атомарность, контекст расписания/записи, совместимость replay; независимая проверка: 377 unit/integration, 85 E2E. Результат принят пользователем.    |
| 05.5 — Журнал записей и управление статусами       | Выполнен, принят | Защищённые журнал/карточка, статусы и история, версии/ABA, совместимость клиентской отмены/replay; исправление независимого замечания, 454 unit/integration и 103 E2E. |
| 05.6 — Ручное создание записи                      | Принят           | ADMIN/SPECIFIC/ANY, общий booking engine, строгая идемпотентность, fragment-ссылка, recovery потерянного ответа, без Telegram/outbox.                                  |
| 05.7.1 — Исправление имени и телефона              | Принят           | Strict DTO, Appointment FOR UPDATE/version, гонки со статусом/отменой, безопасный unknown outcome; 538 unit/integration и 124 E2E.                                     |

## Изменённые файлы 05.1

Всего 31 файл (изменённые и новые; без generated/test artifacts):

- `.env.example`
- `README.md`
- `docs/admin-auth.md`
- `docs/architecture.md`
- `docs/decisions/0005-admin-authentication-and-sessions.md`
- `docs/progress.md`
- `docs/roadmap.md`
- `package-lock.json`
- `package.json`
- `scripts/admin-terminal.ts`
- `scripts/admin.ts`
- `src/app/admin/actions.ts`
- `src/app/admin/login/page.tsx`
- `src/app/admin/page.tsx`
- `src/app/globals.css`
- `src/components/admin/login-form.tsx`
- `src/components/admin/logout-button.tsx`
- `src/modules/auth/policy.ts`
- `src/modules/auth/policy.unit.test.ts`
- `src/modules/auth/server/admin-operator.ts`
- `src/modules/auth/server/admin-terminal.unit.test.ts`
- `src/modules/auth/server/auth-service.ts`
- `src/modules/auth/server/password.ts`
- `src/modules/auth/server/password.unit.test.ts`
- `src/proxy.ts`
- `src/server/admin/boundary.ts`
- `src/server/admin/boundary.unit.test.ts`
- `src/server/admin/index.ts`
- `src/server/public/security.ts`
- `tests/e2e/admin.spec.ts`
- `tests/integration/admin-auth.test.ts`

## Этап 05.3 — административное расписание (2026-08-31)

**Этап 05.3 прошёл независимую проверку и принят пользователем.** Исходный HEAD реализации: `4f0da8aa40c821a8b1d79e4e39e2dd33c9d72f9a`, ветка main, исходное дерево чистое. После приёмки пользователь разрешил фиксацию и push в origin/main без force; ADR-0008 переведён в Accepted. Ограничения и открытый вопрос npm audit сохранены.

### Реализовано

- Защищённый `/admin/schedule`, выбор мастера, ссылки из каталога, явные зона бизнеса и активность. Новый мастер без автоматического графика.
- Недельные рабочие часы и перерывы, несколько интервалов с минутной точностью, пустые дни, клавиатурное добавление/удаление и явное сохранение.
- DAY_OFF, CUSTOM_HOURS, изменение даты/типа/интервалов и подтверждённое удаление. Особые часы заменяют только работу, недельные перерывы сохраняются; выходной не преобразует неиспользуемые часы. Уточнена старая формулировка ADR-0002.
- Атомарные транзакции, общий с 05.2 замок и Master.version, конфликты устаревшей недели, занятости даты, редактирования/удаления; повторная проверка доступа после ожидания замка. Принадлежность исключения проверяется по ID и мастеру.
- Строгие серверные схемы, нормализация существующим доменным алгоритмом, проверка каждого датированного DST-интервала до слияния, ограниченные чтения и массивы. При неизвестном исходе автоматического повтора нет, черновик сохраняется для сверки.
- Существующие записи, снимки, история и ссылки не изменяются. Новая доступность читает текущий график; устаревшее создание отклоняется, успешный replay возвращает прежнюю запись. Scheduling, booking, согласование условий и exclusion constraint не переписывались.
- Схема/миграции и зависимости не менялись. Решение — [ADR-0008](decisions/0008-admin-schedule-concurrency.md), инструкция — [admin-schedule.md](admin-schedule.md).

### Проверки

- Полный Vitest на настоящем PostgreSQL: **300 тестов прошли**, 25 файлов. Новые проверки включают границы/даты/минуты, нормализацию, пустую работу, независимость мастеров, особые часы с перерывами, смену типа/удаление, DB-trigger rollback после начала записи, гонки, авторизацию/Origin/подстановки, отзыв во время ожидания каждого изменения, DST, SPECIFIC/ANY и replay со старыми снимками.
- Полный Playwright на итоговой production-сборке: **65 сценариев прошли** (12 новых для 05.3). Проверены все три новые Action через HTTP, отзыв/истечение/отключение/отсутствие сессии, чужой Origin, подстановка полей, GET без записи, потеря реального успешного ответа, SPECIFIC/ANY после закрытия дня и восстановление публичного replay с прежней записью/историей/ссылкой.
- Успешны `npm run build` (Prisma generate → Next web → worker), последующий `npm run typecheck`, `npm run lint`, `npm run format:check` и `git diff --check`. Build/Prisma generate и typecheck запускались последовательно.
- Браузерные тесты используют реальные Server Actions и отдельную PostgreSQL-БД; моки только для потери/задержки сети. Проверены размеры 360/390/1440 px, отсутствие горизонтального переполнения, клавиатура/фокус, новая форма без графика, ожидание, конфликт и неопределённый ответ. Снимки недели и исключений на mobile/desktop просмотрены.
- Первоначальные неуспешные прогоны не засчитаны: исправлены синтаксис JSX, тип заголовков E2E и фикстура позиции мастеров; затем уточнён locator alert, чтобы исключить служебный Next route-announcer. Во втором E2E выявлено краткое одновременное отображение ожидания и успеха — исправлено в UI, локатор результата уточнён.
- Каждый runner создавал случайную zaprosto_test_* БД и удалял только её. Миграции/фикстуры не запускались на рабочей БД. Реальные аккаунты и Docker volumes не изменялись. Итоговая read-only проверка: **0 временных тестовых БД**, **порт 3108 свободен**, HEAD совпадает с исходным.

### Ограничения

- Проверяется Chromium на локальном HTTP. Не проверялись Safari/Firefox, физические устройства, screen reader, production TLS/reverse proxy, внешняя проверка безопасности и новая Docker image-сборка.
- Стандартные shell и просмотр PNG не работают из-за ошибки Windows sandbox. Команды выполнены через одобренный запуск вне песочницы; снимки прочитаны без изменения тем же способом. Отдельный интерактивный сеанс встроенного браузера не проводился.
- Недельный график не проверяется на все будущие DST-даты: конкретную дату строго проверяет scheduling. Границы CUSTOM_HOURS и действующие недельные перерывы проверяются для указанной даты; DAY_OFF их не преобразует. Ночные смены, 24:00 и секунды не поддерживаются.
- Общий Master.version намеренно вызывает конфликт и после изменения карточки/порядка. Прямой SQL в обход протокола не защищает старые формы. Уже выполняющийся публичный запрос и изменение расписания могут завершиться в допустимом порядке; существующие визиты автоматически не исправляются.
- Черновик хранится только в памяти страницы; смена мастера/месяца, навигация или закрытие формы его теряет. При неизвестном исходе требуется ручная сверка, exactly-once для административных операций не заявляется.
- Технические пределы: 16 рабочих интервалов и 16 перерывов на день, 16 особых часов, месяц исключений и 50 мастеров на страницу. Данные сверх предела не обрезаются.
- **npm audit: прежние 3 high в Prisma → @prisma/config → deepmerge-ts остаются открытым вопросом 05.1.** Повторный audit, обновления зависимостей и force fix не выполнялись. Сохранены предупреждения pg 8 о client.query и next start при output=standalone.

### Точка продолжения

Этап 05.3 принят пользователем после независимой проверки. Разрешены фиксация только файлов 05.3 и push в origin/main после проверки актуальности ветки, форматирования и staged diff. Ограничения проверок перечислены выше. Следующий этап не начат и без отдельного поручения не начинается.

### Изменённые файлы 05.3

23 файла без generated/test artifacts:

- `README.md`
- `docs/admin-auth.md`
- `docs/admin-catalog.md`
- `docs/admin-schedule.md`
- `docs/architecture.md`
- `docs/decisions/0002-choose-application-stack-and-architecture.md`
- `docs/decisions/0008-admin-schedule-concurrency.md`
- `docs/progress.md`
- `docs/roadmap.md`
- `src/app/admin/page.tsx`
- `src/app/admin/schedule-actions.ts`
- `src/app/admin/schedule/page.tsx`
- `src/app/globals.css`
- `src/components/admin/catalog-editor.tsx`
- `src/components/admin/navigation.tsx`
- `src/components/admin/schedule-editor.tsx`
- `src/modules/scheduling/domain/admin-input.ts`
- `src/modules/scheduling/domain/admin-input.unit.test.ts`
- `src/modules/scheduling/server/admin-schedule.ts`
- `src/server/admin/schedule-boundary.ts`
- `src/server/admin/schedule.ts`
- `tests/e2e/admin-schedule.spec.ts`
- `tests/integration/admin-schedule.test.ts`

## Этап 05.4 — настройки времени и горизонта (2026-08-31)

**Этап 05.4 прошёл независимую проверку и принят пользователем.** Независимые результаты, подтверждённые пользователем: **377 unit/integration и 85 E2E успешно**. ADR-0009 переведён в Accepted. Исходный HEAD реализации: `f8d5df8c002c56e1845cf5a08afd613b04807a04`; исходное дерево чистое. До приёмки commit/push не выполнялись. После приёмки пользователь разрешил фиксацию только файлов 05.4 и push в origin/main после проверки отсутствия расхождения веток, без force. После лимита реализация продолжалась без перезаписи существующих изменений; перед запуском проверок подтверждено отсутствие прежних процессов build/test.

### Реализовано

- Защищённая русскоязычная `/admin/settings`, ссылка в навигации, текущие сохранённые значения, явная кнопка сохранения, ошибки/ожидание/результат и клавиатурный фокус. Горизонт — строго целое 7–90 дней; зона — именованная, по существующему Temporal/scheduling, без произвольных UTC-смещений и ограничения российскими зонами. Значения новой установки Europe/Moscow и 30 дней сохранены.
- Новая миграция добавляет только BusinessSettings.version с default 0 и CHECK >= 0. Оба параметра сохраняются атомарно, версия защищает устаревшие формы и ABA. Смена зоны требует серверного подтверждения; UI показывает старую/новую зоны и последствия. Черновик остаётся при конфликте и неизвестном исходе, автоматического повтора или подстановки свежей версии нет.
- Сохранён общий административный advisory lock; singleton настроек блокируется FOR UPDATE. Активная сессия перепроверяется после обоих ожиданий. Строгий DTO, Origin/CSRF, no-store, nonce-CSP и полная навигация сохранены; GET не меняет данные.
- Все три операции расписания сверяют исходный контекст времени вместе с Master.version. Публичный каталог и доступность читают согласованный контекст в RepeatableRead; создание проверяет его под FOR SHARE в Serializable. Конкурентное изменение настроек после снимка транзакции не разрешает создание по старым данным.
- Новое создание использует booking-v3; исторические booking-v1/v2 hashes и точный payload сохранённых попыток не меняются. Replay выполняется до проверки текущих настроек и возвращает прежнюю запись/ссылку с актуальной зоной отображения. Отсутствие нового поля не разрешает новое создание.
- Публичный UI обновляет зону/даты при конфликте, сохраняет контакты, сбрасывает время и требует явного повторного согласия. Запоздавшая доступность отбрасывается. Неизвестный исход сохраняет исходную пару и не создаёт новую попытку автоматически.
- Существующие UTC-моменты, снимки, история, статусы, защищённые ссылки, локальные часы недели и даты исключений не переписываются. Горизонт остаётся N локальных календарных дней, включая сегодня; строгие DST-правила не меняются.
- Обновлены README, roadmap, архитектура и инструкции; добавлены [инструкция настроек](admin-settings.md) и [ADR-0009](decisions/0009-business-time-context.md). Ручное управление записями, медиа и Telegram не добавлялись.

### Фактически выполненные проверки

- Полный unit/integration через `rtk proxy node --env-file=.env.example scripts/test-postgres.mjs`: **377 тестов, 27 файлов, успешно** (23,09 с), отдельная БД `zaprosto_test_ce952ec256e5480b9366bb86ae258def`. Включены атомарность и принудительный rollback, конкурентные формы/ABA, отзыв доступа после ожидания обоих замков, старое расписание, SPECIFIC/ANY, локальная полночь/горизонт/DST, неизменность данных и исторический replay.
- Полный Playwright через штатный runner `--e2e`: **85 тестов Chromium, успешно** (2,9 мин), отдельная БД `zaprosto_test_f1d8c922255141d79d671c42ca3a102f`. Реальные Server Actions и PostgreSQL; новые сценарии 05.4 включают потерянный успешный ответ настроек, старые формы, запоздавшую доступность, booking-v1/v2/v3 после смены настроек и неизвестный исход до COMMIT без автоматической новой пары.
- После полного прогона по снимкам исправлен только отступ между полем и подсказкой на странице настроек. Затем на итоговом коде **повторно прошли production build web + worker** (`Prisma generate → Next build → worker`) и **отдельный последующий typecheck**. Build/Prisma generate и typecheck выполнялись последовательно.
- После этой CSS-правки на итоговой production-сборке **повторно прошли все 20 E2E настроек** (43,6 с): `rtk proxy node --env-file=.env.example scripts/test-postgres.mjs --e2e tests/e2e/admin-settings.spec.ts`, отдельная БД `zaprosto_test_d985fda18f6a41d4b75712a106de7c08`. Серверная логика после успешных 377 unit/integration не менялась; CSS-правка затрагивает только страницу настроек, поэтому остальные 65 E2E повторно не запускались.
- Успешны `npm run lint`, `npm run format:check` и `git diff --check`. Форматирование и diff проверены после окончательного обновления документации. После lint TypeScript/JavaScript больше не менялись.
- E2E проверяет ширины 360, 390 и 1440 px, отсутствие горизонтального переполнения, клавиатуру, фокус, ошибки и ожидание. Итоговые PNG 360 и 1440 px просмотрены: текст/фокус не пересекаются, обрезаний не обнаружено. Снимки остаются локальными артефактами в игнорируемом `test-results/`.
- После runner read-only проверка подтвердила **0 временных тестовых БД**, порт **3108 свободен**, HEAD неизменен. Все тестовые миграции, фикстуры и записи выполнялись только в случайных БД штатного runner; рабочая БД, реальные аккаунты и Docker volumes не изменялись.
- Первый build завершился ошибкой из-за старого контракта тестовой композиции public boundary; контракт исправлен, последующие сборки успешны. Отклонённое из-за лимита форматирование не запускалось и не засчитано как успешное. Незавершённых или прерванных проверок в итоговых успешных результатах нет.

### Ограничения и открытые вопросы

- Проверен Chromium в локальном HTTP-окружении. Firefox/WebKit, реальные мобильные устройства, screen reader, production HTTPS/reverse proxy/CDN и внешний аудит безопасности не проверялись. Прежние эксплуатационные ограничения 05.1–05.3 сохраняются.
- Новая миграция проверена runner на временных БД с нуля; к рабочей БД не применялась. Обновление существующей рабочей установки и её резервное копирование остаются отдельной операторской процедурой, описанной в инструкции.
- **npm audit: прежние 3 high в Prisma → @prisma/config → deepmerge-ts остаются открытым вопросом 05.1.** Повторный audit, audit fix, обновления зависимостей и deployment не выполнялись. Предупреждения pg 8 о client.query и next start при output=standalone не устранены этим этапом.
- Общая версия консервативно инвалидирует контекст даже при изменении только горизонта или сохранении прежних значений. Прямой SQL в обход протокола версий не защищён от ABA. Отзыв сессии после финальной проверки не отменяет уже выполняющуюся операцию.
- Административный черновик хранится в памяти страницы; навигация его теряет. При конфликте или неизвестном исходе используется отдельная вкладка для сверки. Exactly-once для административных сохранений не заявляется.

### Изменённые файлы 05.4

43 файла, включая новые, без generated/test artifacts:

- `README.md`
- `docs/admin-auth.md`
- `docs/admin-catalog.md`
- `docs/admin-schedule.md`
- `docs/admin-settings.md`
- `docs/architecture.md`
- `docs/booking-service.md`
- `docs/decisions/0009-business-time-context.md`
- `docs/progress.md`
- `docs/public-booking.md`
- `docs/roadmap.md`
- `prisma/migrations/20260831200000_settings_version/migration.sql`
- `prisma/schema.prisma`
- `src/app/admin/settings-actions.ts`
- `src/app/admin/settings/page.tsx`
- `src/app/globals.css`
- `src/components/admin/navigation.tsx`
- `src/components/admin/schedule-editor.tsx`
- `src/components/admin/settings-editor.tsx`
- `src/components/booking/booking-flow.tsx`
- `src/modules/booking/domain/booking-input.ts`
- `src/modules/booking/server/booking-security.ts`
- `src/modules/booking/server/booking-service.ts`
- `src/modules/booking/server/types.ts`
- `src/modules/scheduling/domain/admin-input.ts`
- `src/modules/scheduling/server/admin-schedule.ts`
- `src/modules/settings/domain/input.ts`
- `src/modules/settings/domain/input.unit.test.ts`
- `src/modules/settings/server/context.ts`
- `src/server/admin/settings-boundary.ts`
- `src/server/admin/settings.ts`
- `src/server/public/boundary.ts`
- `src/server/public/catalog.ts`
- `src/server/public/index.ts`
- `tests/e2e/admin-settings.spec.ts`
- `tests/e2e/booking-terms.spec.ts`
- `tests/e2e/booking.spec.ts`
- `tests/integration/admin-catalog.test.ts`
- `tests/integration/admin-schedule.test.ts`
- `tests/integration/admin-settings.test.ts`
- `tests/integration/booking-service.test.ts`
- `tests/integration/booking-terms.test.ts`
- `tests/integration/public-boundary.test.ts`

### Точка продолжения 05.4

Этап 05.4 принят пользователем после независимой проверки: 377 unit/integration и 85 E2E успешно. ADR-0009 — Accepted. Разрешены фиксация только перечисленных файлов 05.4 коммитом `feat: add business time settings and booking context protection` и push в origin/main после проверки форматирования, staged diff и актуальности ветки. При расхождении с origin/main остановиться; force push запрещён. После отправки проверить HEAD непосредственно на GitHub и чистоту рабочего дерева. Ограничения и открытый вопрос npm audit сохранены. Рабочую БД не изменять, миграцию к ней не применять. Следующий этап не начат и без отдельного поручения не начинается.

## Этап 05.5 — журнал записей и управление статусами (2026-08-31)

Исходный HEAD: `b701bb732682d8e638e19e506610e6daa350c2f2`; рабочее дерево было чистым. Выполняется только 05.5; commit/push запрещены до отдельной проверки.

### Реализовано

- Защищённые /admin/appointments и /admin/appointments/[id], навигация и русскоязычный mobile-first интерфейс без календарной библиотеки.
- Сегодня в зоне бизнеса; дата, мастер, статус; отменённые скрыты по умолчанию, доступны отдельно и среди всех статусов. Прошлые/будущие даты не ограничены горизонтом. Список и история по 25, мастера по 50; явные ссылки следующих страниц. Фильтры и страницы сохраняются при переходах.
- День по startsAt в текущей зоне, полуоткрытые границы через существующий getLocalDayInterval, включая DST. Невозможная/неоднозначная полночь даёт ошибку без сдвига.
- Исторические snapshots услуги, активные и неактивные мастера; контакты только за проверенной сессией. Карточка содержит источник, отмену и историю с ролью/ID инициатора. Нет выдачи hashes, сессий, лишних таблиц или генерации клиентской ссылки. GET не изменяет данные.
- Строгие DTO, утверждённая матрица переходов и проверка начала по clock_timestamp() после блокировок. Подтверждение отмены, причина до 1000 символов и предупреждение о ручном контакте с клиентом. UPDATE статуса и INSERT истории с ADMIN и ID из сессии атомарны.
- Appointment.version добавлена новой миграцией, без изменения старых миграций/данных. Версию увеличивают оба пути смены статуса — администратор и клиентская отмена. Конфликты, ABA, повтор и бизнес-контекст проверяются после замков advisory → settings FOR SHARE → appointment FOR UPDATE с повторной авторизацией. Переполнение откатывается.
- Неизвестный исход не повторяется; причина остаётся в форме, повтор заблокирован, есть сверка в новой вкладке. Успех полностью перечитывает карточку с новым nonce. booking-v1/v2/v3 и исходные защищённые ссылки совместимы.
- Добавлены [ADR-0010](decisions/0010-admin-appointment-statuses.md) со статусом Proposed и [инструкция журнала](admin-appointments.md); обновлены архитектура, roadmap, README и связанные инструкции.

### Проверки первоначальной реализации

- Успешны format:check и lint. Для двух компонентов с полной навигацией добавлено локальное исключение no-html-link-for-pages с объяснением nonce-CSP; клиентский router не вводился.
- Итоговый production build web + worker успешен: Prisma generate → Next build → worker. Отдельный последующий typecheck успешен; одновременного запуска с build/generate не было.
- Полный unit/integration на итоговом коде: **444 теста, 29 файлов, успешно**, 38,29 с; штатный runner, отдельная БД `zaprosto_test_b36ba263470c468ca90a1be94a58a9fb`. Предыдущий полный прогон также успешен: 444 теста, 38,72 с, БД `zaprosto_test_306cb699d1f240b9920d8a08a711e471`.
- До финальных двух CSS-отступов все **18 новых E2E успешны** (37,8 с), БД `zaprosto_test_1e3ced6621d24ffea77e8bb773d2f7ad`. Проверены реальные Actions/БД, конфликты, ABA, потерянный успешный ответ, неизвестный исход до COMMIT, отсутствие автоповтора, direct Action/Origin/DTO, сессии и отзыв во время ожидания.
- Полный E2E на итоговой production-сборке: **103 теста Chromium успешно**, 3,4 мин; штатный runner, отдельная БД `zaprosto_test_b4829e8d5ca246b292189c5df9fa9507`. Все 18 новых сценариев и прежние 85 прошли; прерванных проверок в итоговых результатах нет.
- Итоговые PNG журнала и карточки 360 и 1440 px из полного E2E просмотрены после исправления отступов; обрезаний и проблем компоновки не обнаружено. E2E проверяет также 390 px, отсутствие горизонтального переполнения, клавиатуру, фокус, пустые состояния и ошибки. Артефакты остаются в игнорируемом test-results.
- В первоначальном build выявлена отсутствующая аннотация массива в новом тесте; исправлена. Первоначальный E2E завершился 3 успешными и 14 неуспешными тестами из-за селекторов label/select и неоднозначного role=alert (Next route announcer); исправлены тестовые селекторы. Первоначальный lint выявил правило полной навигации и временный файл скрипта; исправлено, временный файл удалён. Эти неуспешные прогоны не засчитаны как успешные.
- Песочница Windows не запускалась (setup refresh helper error); shell-операции выполнялись через проверенное повышение прав, rtk. apply_patch и view_image также не сработали; файлы изменялись через Node в каталоге проекта, снимки прочитаны через shell. Все тесты запускались только штатным runner.

- После runner read-only проверка подтвердила **0 временных тестовых БД** и свободный порт **3108**. HEAD остаётся исходным; package.json/package-lock.json не менялись. Все миграции и фикстуры выполнялись только в случайных БД, созданных runner.
- Итоговые format:check и git diff --check выполнены после завершения документации.

### Исправление замечания независимой проверки 05.5 (2026-09-01)

- Независимая проверка выявила исчерпание штатного пула: повторный getActiveAdmin через основной клиент вызывался из callback открытой RepeatableRead-транзакции. При 10 одновременных чтениях 10 соединений удерживались транзакциями, а финальная авторизация ожидала свободное соединение.
- Boundary теперь сначала завершает RepeatableRead-транзакцию и освобождает соединение, затем отдельно проверяет актуальные сессию и аккаунт. Начальная авторизация и согласованный снимок сохранены; результат остаётся локальным до успешной финальной проверки. При потере доступа возвращается только UNAUTHORIZED, при ошибке запроса — только UNAVAILABLE, без контактов.
- Размер пула, maxWait/timeout, логика статусов и миграции не изменены. ADR-0010 уточнён; статус Proposed и все ограничения сохранены.
- Добавлены 10 интеграционных регрессий: по 10 одновременных реальных чтений журнала и карточек с барьером после чтения; отзыв, истечение сессии и отключение аккаунта между чтением и финальной авторизацией для обоих методов; ошибка финального запроса. Проверяются начальная авторизация, факт чтения, отсутствие открытой транзакции перед финальной проверкой и отсутствие персональных данных в ответах об отказе.
- Регрессии проверены на исходном коде: **8 ожидаемых падений, 2 успешных**, БД `zaprosto_test_933fddefbbd94c44b8a53b1a622c2faa`. Параллельные list/detail заняли 10 344/10 071 мс и вернули ошибки; в шести сценариях изменения доступа обнаружена одна открытая транзакция. Этот прогон подтверждает дефект и не засчитан как успешная проверка.
- После исправления все **67 тестов административных записей успешны**, 8,44 с, БД `zaprosto_test_7e17ea0290d0454a9b30e4e31b80149f`.
- Итоговые format:check и lint успешны. Production build web + worker успешен; Prisma generate/build полностью завершены до отдельного успешного typecheck.
- Полный unit/integration после исправления: **454 теста, 29 файлов, успешно**, 45,89 с; штатный runner, отдельная БД `zaprosto_test_2e7536b44df64148931dee5aefd05a58`.
- Полный E2E на итоговой production-сборке: **103 теста Chromium успешно**, 3,4 мин; штатный runner, отдельная БД `zaprosto_test_9ffb438413514d44bab2c970e4eb3ba5`.
- Остановленный лимитом прогон на БД `zaprosto_test_f234a028d492457596207b972e8034db` не был засчитан. Перед повтором подтверждено отсутствие его процесса и временной БД.

### Ограничения и открытые вопросы

- Миграция применялась только к случайным тестовым БД. Рабочая БД, реальные аккаунты и Docker volumes не изменялись. Обновление рабочей установки/резервное копирование остаются отдельной операторской процедурой.
- Проверен Chromium в локальном HTTP-окружении; Firefox/WebKit, реальные устройства, screen reader, production HTTPS/reverse proxy/CDN и внешний аудит безопасности не проверялись.
- **Прежние 3 high npm audit в Prisma → @prisma/config → deepmerge-ts остаются открытыми.** Повторный audit, audit fix, изменение зависимостей и deployment не выполнялись. Предупреждения pg client.query и next start при standalone остаются.
- Прямой SQL без соблюдения протокола версий не защищает от ABA. Отзыв после последней проверки не отменяет завершённое действие; ранее показанные контакты не стираются из открытой вкладки. Долговременный снимок между страницами offset-пагинации не обещается.
- Черновик причины хранится только в памяти страницы и теряется при навигации. При неизвестном исходе нужно сверить карточку; отдельной exactly-once доставки нет, повтор старого DTO не создаёт второе событие.
- Ручное создание, редактирование контактов/услуги, перенос, медиа, Telegram/outbox и следующий этап не реализовывались.

### Изменённые файлы 05.5

30 файлов, включая новые; generated/test artifacts не входят:

- `README.md`
- `docs/admin-appointments.md`
- `docs/admin-auth.md`
- `docs/admin-catalog.md`
- `docs/admin-schedule.md`
- `docs/admin-settings.md`
- `docs/architecture.md`
- `docs/booking-service.md`
- `docs/decisions/0010-admin-appointment-statuses.md`
- `docs/progress.md`
- `docs/public-booking.md`
- `docs/roadmap.md`
- `prisma/migrations/20260831220000_appointment_version/migration.sql`
- `prisma/schema.prisma`
- `src/app/admin/appointment-actions.ts`
- `src/app/admin/appointments/[id]/page.tsx`
- `src/app/admin/appointments/page.tsx`
- `src/app/admin/page.tsx`
- `src/app/globals.css`
- `src/components/admin/appointment-status-editor.tsx`
- `src/components/admin/appointments-error.tsx`
- `src/components/admin/navigation.tsx`
- `src/modules/appointments/domain/admin-input.ts`
- `src/modules/appointments/domain/admin-input.unit.test.ts`
- `src/modules/appointments/server/admin-appointments.ts`
- `src/modules/appointments/server/client-appointment-service.ts`
- `src/server/admin/appointments-boundary.ts`
- `src/server/admin/appointments.ts`
- `tests/e2e/admin-appointments.spec.ts`
- `tests/integration/admin-appointments.test.ts`

### Точка продолжения 05.5

Этап 05.5 принят пользователем после исправления замечания независимой проверки: 454 unit/integration и 103 E2E успешны, ADR-0010 — Accepted. Разрешены фиксация ровно 30 перечисленных файлов коммитом `feat: add admin appointment journal and status management` и обычный push в origin/main без force. Следующий этап не начинать. Рабочую БД не изменять; миграцию к ней не применять автоматически. Ограничения и открытый npm audit сохранены.

## Этап 05.6 — ручное создание записи администратором (2026-09-01)

Исходный HEAD и origin/main: `f32f86d0e10156780c6cbd5aa0c0f9923718d4f6`, ветка `main`, рабочее дерево было чистым. Выполняется только 05.6; commit/push запрещены до независимой проверки. Prisma-схема и миграции не меняются, рабочая БД и Docker volumes не используются для тестов.

### Реализовано

- Защищённый `/admin/appointments/new` и переход «Создать запись» из журнала. Четыре mobile-first шага: активная услуга/назначенный мастер или ANY, локальная дата/актуальные окна, имя/существующая нормализация российского телефона, итог и реальный checkbox подтверждения.
- Начальная форма и availability читаются в RepeatableRead с начальной авторизацией; транзакция завершается до отдельной финальной проверки доступа, сохраняя исправление исчерпания пула 05.5. Возвращаются только активные услуги и активные подходящие мастера из согласованного снимка.
- Публичный BookingService и новый AdminBookingService используют общий внутренний транзакционный engine, а не публичный HTTP boundary. Сохранены service terms, business context, утверждённые selectAnyMaster/checkMasterInterval, Serializable retry и PostgreSQL exclusion constraint.
- Запись сохраняется как SCHEDULED/source=ADMIN/masterSelection=SPECIFIC или ANY, с обязательным BookingRequest, snapshots услуги и атомарной начальной историей previousStatus=null/newStatus=SCHEDULED/changedBy=ADMIN/changedByAdminId из проверенной сессии.
- Сессия проверяется до операции, через FOR SHARE после значимых ожиданий и ещё раз boundary после транзакции. Потеря доступа откатывает либо подавляет результат без контактов и токена; конкурентный отзыв во время exclusion-ожидания покрыт реальной PostgreSQL-регрессией.
- Отдельный strict DTO требует confirmed=true, UUID, 256-битный cancellation token и отпечатки условий/контекста; лишние и серверные поля отклоняются. Admin fingerprint стабилен после нормализации и связывает все предметные поля; неверный payload или токен с прежним ключом даёт IDEMPOTENCY_CONFLICT.
- До первого create точный исходный payload сохраняется в sessionStorage вкладки. Неизвестный результат блокирует новую пару и разрешает только replay исходного запроса. Через 30 минут контакты удаляются, marker токена сохраняет возможность сверки; localStorage/cookie не используются.
- В БД хранится только cancellationTokenHash. Raw-токен возвращается только при подтверждённом успехе/replay, не включается в ошибки, query/path/server HTML/RSC или логи. Абсолютная ссылка формируется браузером как текущий origin + `/appointment#TOKEN`.
- Успешный экран показывает запись, безопасный plain-text с услугой, мастером, локальными датой/временем, ссылкой и предупреждением; отдельные кнопки копируют текст/ссылку, Clipboard fallback выделяет поле вручную, есть переход в карточку. Клиентская ссылка просматривает и отменяет ADMIN-запись; отмена освобождает слот и не меняет source.
- TelegramLink, NotificationOutbox, Telegram/SMS, редактирование, перенос, напоминания, медиа, deployment и 05.7 не добавлены. Новых зависимостей, календарной библиотеки, Prisma-изменений и миграций нет.
- Добавлены [инструкция](admin-appointment-creation.md) и [ADR-0011](decisions/0011-admin-manual-appointment-creation.md) со статусом Accepted после независимой приёмки; обновлены архитектура, roadmap, README и документация журнала.

### Завершённые целевые проверки до полного прогона

- Ранний typecheck и production build web + worker завершились успешно; build и typecheck не выполнялись одновременно.
- Новые unit: **19/19 успешно** — strict DTO, телефон +7/8 и нормализация, лишние поля, SPECIFIC/ANY, fingerprint, безопасный confirmation text и sessionStorage lifecycle.
- Новые integration в отдельной случайной PostgreSQL БД: **16/16 успешно**. Проверены ADMIN/history/snapshots, SPECIFIC/ANY, активный каталог, расписание/terms/context, Origin/DTO, replay/конфликты, concurrent exclusion, сессии и отзыв во время ожидания, отсутствие raw-токена/Telegram/outbox, клиентский просмотр/отмена и освобождение слота.
- Новые E2E на production-сборке в отдельной случайной БД: **10/10 успешно**. Проверены полные SPECIFIC/ANY, телефон/empty/stale/terms, double click, неизвестный исход до/после COMMIT, safe replay, copy/fallback, карточка/клиентская отмена, прямые Actions, 360/390/1440, клавиатура и фокус.
- PNG формы 360 и 1440 px прочитаны и просмотрены: переполнения, обрезания, наложения и недоступного управления не обнаружено. Снимки находятся только в игнорируемом `test-results/`.
- Эти целевые результаты не заменяют обязательный полный последовательный прогон. После восстановления проверено: незавершённых Node/Next/Playwright/runner процессов нет, порт 3108 свободен, временных `zaprosto_test_*` БД нет.

### Полный обязательный прогон

Последовательность 1–8 завершена полностью; build и typecheck не пересекались:

1. `rtk npm run format:check` — успешно, все файлы соответствуют Prettier.
2. `rtk npm run lint` — итоговый прогон успешно, 0 ошибок и предупреждений. Первый незасчитанный запуск обнаружил три ошибки: два правила полной навигации и синхронный setState в effect. Добавлены локальные объяснённые исключения по принятой nonce-CSP политике, лишний setState удалён; бизнес-логика не менялась.
3. `rtk npm run build` — успешно: Prisma Client 7.10.0, Next.js 16.3.3 production build, маршрут `/admin/appointments/new` и worker TypeScript.
4. `rtk npm run typecheck` — отдельно после завершения build, успешно.
5. Полный unit/integration runner — **33 файла, 489/489 тестов успешно**, 39,87 с; случайная БД `zaprosto_test_c30c3dbf810342018098bd0772376c0b`, все 7 миграций применены только к ней, runner завершился с code 0.
6. Полный Playwright/Chromium — **113/113 E2E успешно**, 3,1 мин; production-сборка, случайная БД `zaprosto_test_ecf67ff1530744f3ac78d1bda6155e6e`, runner завершился с code 0.
7. `rtk git diff --check` — успешно, вывода об ошибках нет.
8. `rtk docker compose config` — успешно; конфигурация db/web/worker валидна, сервисы и volumes командой не изменялись.

Полный E2E включает все 10 новых сценариев 05.6 и прежние 103 регрессии. Ожидаемые server-side сообщения об отклонённом чужом Origin, предупреждение pg concurrent client.query и предупреждение Next.js для `next start` при standalone не привели к падению. Никакие частичные или первоначально неуспешные прогоны не засчитаны.

После полного runner повторный read-only контроль подтвердил: **0 временных `zaprosto_test_*` БД**, порт **3108 свободен**, проектных Node/Next/Playwright/test-runner процессов нет. Рабочая БД только опрашивалась по системному каталогу, данные/миграции/volumes не изменялись. В 29 файлах diff нет явных секретов; `package.json`, `package-lock.json`, Prisma schema/migrations, generated-файлы и `test-results` в diff отсутствуют.

### Изменённые файлы 05.6

29 файлов, включая этот журнал; generated/test artifacts не входят:

- `README.md`
- `docs/admin-appointment-creation.md`
- `docs/admin-appointments.md`
- `docs/architecture.md`
- `docs/decisions/0011-admin-manual-appointment-creation.md`
- `docs/progress.md`
- `docs/roadmap.md`
- `src/app/admin/appointment-actions.ts`
- `src/app/admin/appointments/new/page.tsx`
- `src/app/admin/appointments/page.tsx`
- `src/app/globals.css`
- `src/components/admin/appointment-create-flow.tsx`
- `src/modules/appointments/domain/admin-confirmation.ts`
- `src/modules/appointments/domain/admin-confirmation.unit.test.ts`
- `src/modules/appointments/domain/admin-create-input.ts`
- `src/modules/appointments/domain/admin-create-input.unit.test.ts`
- `src/modules/auth/server/auth-service.ts`
- `src/modules/booking/client/admin-attempt-storage.ts`
- `src/modules/booking/client/admin-attempt-storage.unit.test.ts`
- `src/modules/booking/domain/booking-input.ts`
- `src/modules/booking/server/admin-booking-service.ts`
- `src/modules/booking/server/booking-catalog.ts`
- `src/modules/booking/server/booking-engine.ts`
- `src/modules/booking/server/booking-service.ts`
- `src/server/admin/appointment-creation-boundary.ts`
- `src/server/admin/appointments.ts`
- `src/server/public/catalog.ts`
- `tests/e2e/admin-appointment-create.spec.ts`
- `tests/integration/admin-appointment-creation.test.ts`

### Ограничения и точка продолжения

- Проверен Chromium в локальном HTTP-окружении. Firefox/WebKit, физические устройства, screen reader, production HTTPS/reverse proxy/CDN и внешний аудит безопасности не проверялись.
- Существующие предупреждения pg о concurrent client.query и Next.js next start при output=standalone не исправляются этим этапом. Прежние 3 high npm audit в цепочке Prisma остаются открытыми; audit, force fix и зависимости не запускались/не менялись.
- sessionStorage — вкладочный recovery: после TTL контакты намеренно удаляются. Уже показанные сведения не стираются после отзыва; отзыв непосредственно после последней успешной проверки не отменяет уже завершённый COMMIT, но boundary не выдаёт его результат и исходная пара остаётся для безопасной сверки.
- Этап 05.6 принят после независимой проверки: production build, typecheck, lint, format:check, git diff --check и `docker compose config` успешны; полный unit/integration runner — **489/489**, полный E2E Chromium — **113/113**; интерфейс на 360 и 1440 px просмотрен. Временных тестовых БД и процессов нет, рабочая БД, Prisma-схема, миграции, Docker volumes и зависимости не изменялись. ADR-0011 переведён в Accepted. Разрешены один коммит `feat: add admin manual appointment creation` ровно из 29 перечисленных файлов и обычный push в origin/main без force. Следующий этап 05.7 не начинать.

## Этап 05.7.1 — административное исправление имени и телефона (2026-09-01)

Исходный HEAD и origin/main: 75b7d0db07d6db68e9ffd8a34e73bf3138518372, ветка main, рабочее дерево было чистым. Этап прошёл независимую проверку и принят пользователем; разрешены фиксация ровно 17 перечисленных файлов коммитом `feat: add admin appointment contact correction` и обычный push в origin/main без force. Рабочая БД, реальные данные и Docker volumes не используются.

### Реализовано

- В карточку /admin/appointments/[id] добавлен редактор актуальных имени и телефона для SCHEDULED, COMPLETED и NO_SHOW. Для CANCELLED показано неизменяемое историческое состояние без формы.
- Отдельный strict DTO принимает только UUID записи, ожидаемую Appointment.version, имя и телефон. Переиспользуются существующие trim/min/max и российская нормализация +7XXXXXXXXXX; дополнительные и серверные поля отклоняются.
- Origin и административная сессия проверяются сервером. ReadCommitted блокирует только Appointment FOR UPDATE, повторно проверяет доступ, перечитывает version/status и перед UPDATE удерживает сессию/аккаунт FOR SHARE. Advisory lock и BusinessSettings не используются.
- Имя, телефон и version+1 меняются одним UPDATE. Status/source/service/master/time/snapshots/token hash/BookingRequest/cancellation/history не меняются. AppointmentStatusHistory, TelegramLink и NotificationOutbox не создаются.
- Две формы, ABA, административный статус и клиентская отмена сериализуются через Appointment.version. Если исправление побеждает, последующая отмена сохраняет исправленные контакты; если отмена побеждает, старая форма конфликтует, а свежая получает EDIT_NOT_ALLOWED.
- UI показывает нормализованный телефон до отправки, блокирует отсутствие реального изменения, сохраняет черновик при конфликте/unknown outcome и не подставляет новую version. Неизвестный результат не повторяется; доступны сверка в новой вкладке и явное полное перечитывание перед новой попыткой.
- После подтверждённого успеха выполняется полная навигация с новым nonce и сообщением об успехе. Контакты не попадают в URL/query/path/cookie/localStorage; защищённая fragment-ссылка и public/admin replay показывают актуальные значения той же записи.
- ADR-0012 принят после независимой проверки. Prisma-схема, миграции, package.json/package-lock.json и зависимости не менялись. Изменение параметров визита, перенос, Telegram/outbox и 05.7.2 не реализованы.

### Целевые проверки до полного прогона

- Ранние lint и typecheck успешны; production build web + worker успешен. Build и typecheck не выполнялись одновременно.
- Новые/связанные unit: **40/40 успешно**.
- Целевой PostgreSQL integration-файл: итоговые **86/86 успешно** в случайной БД. Первый прогон дал 84 успешных и два падения из-за неверного ожидаемого общего счётчика записей; исправлены только ожидания, повтор полностью успешен.
- Целевой Chromium-файл: новые сценарии контактов успешны. Первый полный запуск выявил шесть неоднозначных тестовых локаторов без дефекта приложения; после разведения feedback-класса и уточнения области клиентского status все шесть повторно прошли. Полный итоговый runner зафиксирован ниже.

### Полный обязательный прогон

Последовательность 1–8 завершена полностью; build и typecheck не пересекались:

1. `rtk npm run format:check` — успешно, все файлы соответствуют Prettier.
2. `rtk npm run lint` — успешно, 0 ошибок и предупреждений.
3. `rtk npm run build` — успешно: Prisma Client 7.10.0, Next.js 16.3.3 production build, web и worker.
4. `rtk npm run typecheck` — отдельно после завершения build, успешно.
5. Полный unit/integration runner — итоговые **34 файла, 538/538 тестов успешно**, 33,17 с; случайная БД `zaprosto_test_bbcfbafd663f405db1d7bbf45809529a`, runner завершился с code 0. Первый незасчитанный запуск дал 537/538 из-за ранее существующей чувствительной к времени fixture просроченной сессии в admin-auth с запасом 1 мс; код после него не менялся, повторный полный запуск прошёл полностью.
6. Полный Playwright/Chromium — **124/124 E2E успешно**, 3,7 мин; production-сборка, случайная БД `zaprosto_test_9b535a5e9fc04fb68449ae7a0d85ec1e`, runner завершился с code 0. Ожидаемые предупреждения pg о concurrent client.query и Next.js о `next start` при standalone остались без падений.
7. `rtk git diff --check` — успешно, вывода об ошибках нет.
8. `rtk docker compose config --quiet` — успешно; конфигурация валидна, сервисы и volumes командой не изменялись.

После runner проверено: временных `zaprosto_test_*` БД нет, порт 3108 свободен, процессов Node/Next/Playwright/test runner этой задачи нет. PNG редактора на 360 и 1440 px прочитаны и просмотрены: горизонтального переполнения, обрезания интерфейса, наложений и недоступного управления не обнаружено; снимки находятся только в игнорируемом `test-results/`.

### Изменённые файлы 05.7.1

17 файлов, включая новые; generated/test artifacts не входят:

- docs/admin-appointments.md
- docs/architecture.md
- docs/decisions/0012-admin-appointment-contact-correction.md
- docs/progress.md
- docs/roadmap.md
- src/app/admin/appointment-actions.ts
- src/app/admin/appointments/[id]/page.tsx
- src/app/globals.css
- src/components/admin/appointment-contact-editor.tsx
- src/components/admin/appointment-status-editor.tsx
- src/modules/appointments/domain/admin-contact-input.ts
- src/modules/appointments/domain/admin-contact-input.unit.test.ts
- src/modules/appointments/domain/admin-input.ts
- src/modules/appointments/domain/admin-input.unit.test.ts
- src/server/admin/appointments-boundary.ts
- tests/e2e/admin-appointments.spec.ts
- tests/integration/admin-appointments.test.ts

### Ограничения и точка продолжения

- ADR-0012 переведён в Accepted после независимой проверки. Пользователь разрешил обычный commit и push ровно перечисленных 17 файлов без force.
- Проверяется Chromium в локальном HTTP; Firefox/WebKit, физические устройства, screen reader, production HTTPS/reverse proxy/CDN и внешний аудит безопасности не проверялись.
- Прямой SQL вне протокола version не защищён от ABA. Отзыв, победивший после финальной FOR SHARE-проверки, сериализуется после уже завершившегося исправления.
- Черновик хранится только в памяти вкладки и теряется при явном перечитывании/навигации. Отдельной exactly-once доставки для административного исправления нет.
- Прежние 3 high npm audit, предупреждение pg concurrent client.query и предупреждение Next.js next start при standalone остаются открытыми и этим этапом не исправляются.
- В коммит 05.7.1 входят ровно перечисленные 17 файлов. 05.7.2, параметры визита, перенос, Telegram/outbox, медиа и deployment не начинать.

## Этап 05.7.2 — административное изменение параметров визита и перенос (2026-09-03)

Исходный HEAD и origin/main: `c7c6d91e1a10eeee895de9498f9b946ea3b3d01a`. Работа выполнена поверх незакоммиченного checkpoint блоков 05.7.2A/B без отката принятой реализации. Этап прошёл независимую проверку и принят пользователем; разрешена фиксация ровно 23 перечисленных файлов одним коммитом и обычный push в origin/main.

### Реализовано

- Отдельные strict DTO для защищённой availability и подтверждённой мутации не принимают `excludeAppointmentId`, `HistoricalServiceTerms`, snapshots, контакты, status/source, tokens/hashes или произвольные дополнительные поля.
- KEEP_CURRENT берёт исторические serviceId/name/price/duration только из Appointment и рассчитывает доступность по сохранённой длительности. CATALOG повторно проверяет expectedServiceTerms и применяет только актуальную активную услугу; одинаковый serviceId не смешивает исторические и актуальные условия.
- SPECIFIC и ANY используют общий scheduling service. Только доверенный серверный путь передаёт excludeAppointmentId; публичная availability и административное создание записи не получили нового ввода или изменённого поведения.
- Транзакционная мутация повторно проверяет административную сессию, удерживает общий порядок блокировок, выполняет SELECT ... FOR UPDATE Appointment, сверяет version/SCHEDULED, business context, каталог, назначения и расписание. Один UPDATE атомарно освобождает старый и занимает новый интервал с version+1; exclusion constraint остаётся окончательной защитой.
- Гонки с другим переносом, контактами, статусом, клиентской отменой, публичным и ручным административным созданием завершаются одним победителем и безопасным конфликтом проигравшего. Неизвестный исход COMMIT не повторяется автоматически.
- В карточку SCHEDULED добавлен редактор исторических/каталожных условий, SPECIFIC/ANY, даты и availability со loading/empty/error, подавлением запоздавших ответов и сбросом устаревшего времени. Перед сохранением показано «Было / Станет» и требуется checkbox.
- После успеха выполняется полная навигация с безопасным `visitUpdated=1`, свежим серверным чтением и CSP nonce. При конфликте/unknown черновик остаётся только в памяти вкладки, старое намерение блокируется и предлагается сверка или полное перечитывание.
- COMPLETED и NO_SHOW не переносятся, но сохраняют отдельное исправление контактов. CANCELLED остаётся единым неизменяемым историческим состоянием.
- Автоматическое уведомление о переносе не отправляется; интерфейс прямо требует связаться с клиентом самостоятельно. TelegramLink/NotificationOutbox не создаются, этап 06 не начат.
- [ADR-0013](decisions/0013-admin-appointment-rescheduling.md) переведён в Accepted после независимой проверки; обновлены бизнес-правила, пользовательский сценарий, архитектура, roadmap, README и инструкция журнала.

### E2E 05.7.2C

Новый Playwright-spec содержит 9 детерминированных Chromium-сценариев:

- KEEP_CURRENT через реальный UI с историческими snapshots, SPECIFIC, «Было / Станет», обязательным подтверждением, полной навигацией, сохранением безопасных filters/historyPage и version+1;
- CATALOG с тем же serviceId и явно актуальными условиями, ANY и детерминированным назначением;
- отдельные неизменяемые состояния COMPLETED, NO_SHOW и CANCELLED;
- конфликт старого черновика после исправления контактов во второй вкладке, без автоматического повтора;
- реальное занятие выбранного слота booking engine перед сохранением и обработка SLOT_UNAVAILABLE со свежей availability;
- потерянный сетевой ответ после успешного COMMIT, блокировка повтора и сверка в новой вкладке;
- loading, управляемый запоздавший availability-ответ, retryable transport error, пустой день, клавиатурный focus и отсутствие горизонтального overflow на 360/390/1440 px;
- whitelist безопасных query-параметров, пустые localStorage/sessionStorage и отсутствие новой feature-cookie;
- честное предупреждение о ручной связи с клиентом.

Первые незасчитанные целевые прогоны обнаружили только неточные локаторы нового теста: accessible name даты включал пояснение, денежный формат — неразрывные пробелы и копейки, а URL полной навигации штатно содержал безопасные дефолтные фильтры. Локаторы уточнены по стабильным id/семантическим значениям; дефектов реализации A/B не найдено.

### Итоговые проверки

Последовательность выполнена без параллельного build/typecheck:

1. `rtk npm run format` — успешно.
2. `rtk npm run format:check` — успешно.
3. `rtk npm run lint` — успешно.
4. `rtk npm run build` — успешно: Prisma generate, Next.js production build и worker.
5. `rtk npm run typecheck` — успешно после build.
6. Полный unit/integration runner — **602/602 теста успешно** на отдельной случайной PostgreSQL-БД.
7. Полный Playwright/Chromium runner — **133/133 E2E успешно** на другой случайной PostgreSQL-БД: прежние 124 регрессии и 9 новых сценариев переноса.
8. `rtk docker compose config` и `rtk git diff --check` — успешно.

Оба runner применили семь существующих миграций только к своим `zaprosto_test_*` БД и удалили их после завершения. Рабочая БД, Prisma-схема, миграции, зависимости и Docker volumes не менялись. Существующий здоровый контейнер `db` не запускался и не останавливался этой задачей. После проверки временных БД и зависших project Node/Next/Playwright/test процессов нет; порты 3000 и 3108 свободны.

### Изменённые файлы 05.7.2

Всего 23 файла, включая новые:

- README.md
- docs/admin-appointments.md
- docs/architecture.md
- docs/business-rules.md
- docs/decisions/0013-admin-appointment-rescheduling.md
- docs/progress.md
- docs/roadmap.md
- docs/user-flows.md
- src/app/admin/appointment-actions.ts
- src/app/admin/appointments/[id]/page.tsx
- src/app/globals.css
- src/components/admin/appointment-reschedule-editor.tsx
- src/modules/appointments/domain/admin-input.ts
- src/modules/appointments/domain/admin-reschedule-input.ts
- src/modules/appointments/domain/admin-reschedule-input.unit.test.ts
- src/modules/appointments/server/admin-appointments.ts
- src/modules/appointments/server/admin-reschedule-form.ts
- src/modules/appointments/server/admin-reschedule-service.ts
- src/modules/scheduling/server/availability-service.ts
- src/modules/scheduling/server/types.ts
- src/server/admin/appointments-boundary.ts
- tests/e2e/admin-appointment-reschedule.spec.ts
- tests/integration/admin-appointment-reschedule.test.ts

### Ограничения и точка продолжения

- ADR-0013 имеет статус Accepted после независимой проверки и пользовательской приёмки этапа.
- Проверялся Chromium в локальном HTTP. Firefox/WebKit, физические устройства, screen reader, production HTTPS/reverse proxy/CDN и внешний аудит безопасности не проверялись.
- Прямой SQL вне протокола Appointment.version не защищён от ABA.
- Автоматическое уведомление клиента, Telegram/outbox, напоминания, медиа и deployment не добавлены.
- Пользователь разрешил отдельную фиксацию ровно перечисленных 23 файлов одним коммитом `feat: add admin appointment rescheduling` и обычный push в origin/main после повторной проверки актуальности ветки.

## Этап 06.1 — архитектура Telegram-уведомлений (2026-09-04)

Исходная чистая база: `592bc8f47e4bba5a95d94b4e8030a308d8765c94`; ветка `main`, HEAD и `origin/main` совпадали.

- Созданы [Proposed ADR-0014](decisions/0014-telegram-notifications.md) и [технический план этапа 06](telegram-notifications-plan.md).
- Зафиксированы long polling, одноразовые deep links, отдельные связи клиента/администратора, надёжный outbox, at-least-once delivery, матрица событий, reminder protocol и разбиение 06.2–06.5.
- Это только архитектурное проектирование: Telegram не реализован, этап 06.2 не начат.
- Код приложения, Prisma schema, миграции, npm-зависимости, Docker и worker не изменялись.
- ADR-0014 остаётся `Proposed`; commit и push в рамках 06.1 не выполнялись.
